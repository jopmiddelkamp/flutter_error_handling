name: flutter_error_handling
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_error_handling_core:
    path: flutter_error_handling_core
  flutter_error_handling_data:
    path: flutter_error_handling_data
  flutter_error_handling_ui:
    path: flutter_error_handling_ui
  get_it:

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints:

flutter:
  uses-material-design: true
  assets:
    - assets/data/
