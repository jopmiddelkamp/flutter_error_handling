name: flutter_error_handling_core
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  freezed_annotation:
  get_it:
  json_annotation:

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  build_runner:
  flutter_lints:
  freezed:
  json_serializable:

flutter:
  uses-material-design: true
